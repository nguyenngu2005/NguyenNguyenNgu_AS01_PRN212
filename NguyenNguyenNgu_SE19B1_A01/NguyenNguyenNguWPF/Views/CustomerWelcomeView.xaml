<UserControl x:Class="NguyenNguyenNguWPF.Views.CustomerWelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#1E1E1E" Foreground="White" FontFamily="Roboto">

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0.0" To="1.0" Duration="0:0:0.4"/>
                    <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                        From="0,20,0,0" To="0,0,0,0"
                                        Duration="0:0:0.4"
                                        DecelerationRatio="0.9"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding WelcomeMessage}"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#4FC3F7"
                       TextAlignment="Center"
                       Margin="0,0,0,15"/>

            <TextBlock Text="Welcome to FU Mini Hotel Management System!"
                       FontSize="40"
                       Foreground="#4FC3F7"
                       FontWeight="Bold"
                       TextAlignment="Center"
                       Margin="0,0,0,10"/>
            <TextBlock Text="You can manage your profile or view available rooms using the menu on the left."
                       FontSize="20"
                       Foreground="AntiqueWhite"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="500"/>

        </StackPanel>
    </Grid>
</UserControl>